/*void setup() {
  size(400, 400);
  background(255);
}

void draw() {
  translate(width/2, height/2);
  
  for (int i = 0; i < 360; i += 10) {
    float angle = radians(i);
    float x1 = cos(angle) * 100;
    float y1 = sin(angle) * 100;
    float x2 = cos(angle * 3) * 150;
    float y2 = sin(angle * 3) * 150;
    
    line(x1, y1, x2, y2);
  }
  
  noLoop();
}
*/
