/*void setup() {
  size(400, 400);
  background(255);
  
  for (int i = 0; i < 100; i++) {
    float x = random(width);
    float y = random(height);
    float radius = random(5, 30);
    float gray = random(255);
    
    fill(gray);
    noStroke();
    ellipse(x, y, radius * 2, radius * 2);
  }
  
  save("random_circles.png");
}
*/
