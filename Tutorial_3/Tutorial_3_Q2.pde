/*
void setup() {
  size(400, 400);
  background(255);
}

void draw() {
  // Set line style
  stroke(0);
  strokeWeight(2);
  noFill();
  
  // Draw the spike pattern
  beginShape();
  
  // Loop to create spikes
  for (int x = 50; x <= 350; x += 30) {
    // Base points at bottom
    vertex(x, 300);
    // Peak of spike
    vertex(x + 15, 200);
    // Next base point
    vertex(x + 30, 300);
  }
  
  vertex(50, 300);
  
  endShape();
  
  noLoop();
}

*/
